# Bayesian Variable Selection for Subgroup Analysis (BVSA)

This repository implements the method proposed in ''Bayesian variable selection on structured logistic-normal mixture models for subgroup analysis''.
